{
  "name": "robot_rescate — robot_ws (ROS2 Jazzy)",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached",
  "workspaceFolder": "/workspace",
  "forwardPorts": [8080, 9090],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-iot.vscode-ros"
      ]
    }
  },
  "postCreateCommand": "cd /workspace/robot_ws2 && rosdep install --from-paths src --ignore-src -r -y && colcon build --symlink-install"
}
